  <%= form_with model: @genre, url: admins_genres_path, local:true do |f| %>

          <%= f.label :ジャンル名, class: "col-sm-4" %>
          <%= f.text_field :name, class: "form-control col-sm-8" %>

          <%= f.submit :登録, class: "btn btn-primary" %>


  <% end %>

　<% @genres.each do |genre| %>

　 <%= genre.name %>

　<% end %>




